# Daftar Perbaikan Bug (Bug Fixes Log)

| Tanggal | Modul / Bagian | Deskripsi Perbaikan |
|---------|-----------------|---------------------|
| 27 April 2026 | Master Data / Makanan | - **Foto tidak tampil**: Memperbaiki masalah symlink `public/storage` yang rusak sehingga foto yang diupload sebelumnya tidak bisa dimuat oleh browser. <br> - **Bug menyimpan foto**: Menambahkan reset form (`$('#form-food')[0].reset()`) pada modal Edit makanan. Hal ini mencegah bug di mana file foto dari makanan sebelumnya yang diedit ikut ter-submit jika user mengedit makanan lain. |
| 27 April 2026 | Sidebar Layout | - **Tampilan Role berantakan**: Menyembunyikan judul menu (`menu-title`) "User Management" dan "Saldo" menggunakan directive `@canany` dan `@can`. Hal ini mencegah munculnya judul menu kosong yang membingungkan bagi pengguna (role) yang tidak memiliki hak akses (permissions) ke dalam menu-menu tersebut. |
| 28 April 2026 | Layout Header (Topbar) | - **Penyederhanaan header menu**: Menghapus seluruh elemen header template seperti search, mega menu, language switcher, notification, fullscreen, settings, dan change password modal. <br> - **Header sesuai kebutuhan operasional**: Menyisakan komponen penting saja yaitu tombol sidebar, icon user, dan aksi logout. |
| 28 April 2026 | Search Modul (Saldo, Departemen, Makanan) | - **Search Transaksi Saldo aktif**: Menambahkan filter backend (`name`, `code_departement`) dan wiring frontend (input id, debounce, query parameter `search` saat pagination). <br> - **Search Departemen List aktif**: Menambahkan filter backend (`name`, `code_departement`, `location`) dan wiring frontend yang konsisten pada ajax pagination. <br> - **Search Makanan lebih akurat**: Mengubah pencarian agar hanya berdasarkan **nama makanan** (`name`) dan tidak lagi mencocokkan `description` sehingga hasil pencarian tidak meleset. |
| 28 April 2026 | Permission, Role, Approval Event, dan Saldo Event | - **Permission seluruh fitur dirapikan**: Menambahkan permission granular untuk Dashboard, Event, Master Data, dan Saldo; lalu assign ke role (`Admin`, `Manager`, `Karyawan`) melalui seeder. <br> - **Akses Karyawan ke Master Data ditutup**: Menu sidebar dan endpoint controller master data kini diproteksi permission sehingga karyawan tidak dapat mengakses master data. <br> - **Flow approval sesuai role/departemen/jabatan**: Aksi approve/reject/close event kini divalidasi di backend berdasarkan role, departemen user, dan posisi jabatan; tombol aksi di detail event hanya ditampilkan jika diizinkan. <br> - **Perbaikan format tanggal detail event**: Tanggal pada modal detail event diformat menjadi gaya Indonesia (contoh: `Selasa, 28 April 2026`) dan tidak lagi menampilkan format invalid. <br> - **Pemotongan saldo saat event selesai**: Pada status `Close by Umum`, sistem kini memotong saldo departemen sesuai total konsumsi event dan otomatis membuat riwayat transaksi pada `log_saldos` (status `keluar`). |
